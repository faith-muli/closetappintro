<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="program.com.closetappintro" >

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".CustomIntro"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogIn"
            android:label="@string/title_activity_main" >
        </activity>
        <activity
            android:name=".Closet49splash"
            android:label="@string/title_activity_closet49splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="VAZI" >
        </activity>
        <activity
            android:name=".SetPreference"
            android:label="@string/title_activity_set_profile" >
        </activity>
        <activity
            android:name=".RecyclerView"
            android:label="@string/title_activity_recycler_view" >
        </activity>
    </application>

</manifest>
